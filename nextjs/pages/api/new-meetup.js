const handler = (req, res) => {
  if (req.method === "POST") {
    const data = req.body;

    const { title, image, address, description } = data;

        // "https://react-learning1-default-rtdb.europe-west1.firebasedatabase.app/meetup.json",
  }
};

export default handler;
